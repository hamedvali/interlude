# Runtime state (created on the user's machine, never committed)
app/.run/
app/state.json
app/state.json.tmp

# Python
__pycache__/
*.py[cod]

# Backups & OS cruft
*.bak.*
.DS_Store

# Raw screen recordings (only the exported GIF belongs in the repo)
*.mov

# Tooling artifacts
.playwright-mcp/
